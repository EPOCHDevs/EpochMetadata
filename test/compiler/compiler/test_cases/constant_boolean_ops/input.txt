# Test comparison operations producing boolean results used in subscripts
src = market_data_source()

# Comparison operations evaluate to True (1) or False (0)
a = 10
b = 5

# These create comparison transforms
comp1 = a > b
comp2 = a < 20

# Use simple constants in subscripts
x = src.c[a]
y = src.v[b]
