src = market_data_source(timeframe="1D")

# Strings in ternary expressions
condition = src.c > src.o
label = "Bullish" if condition else "Bearish"

# Direct string assignment
status = "Active"
market = "NYSE"
