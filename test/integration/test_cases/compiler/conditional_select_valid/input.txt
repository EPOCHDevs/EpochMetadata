# Test valid conditional_select usage
src = market_data_source()
rsi_val = rsi(period=14)(src.c)
oversold = rsi_val < 30
overbought = rsi_val > 70
regime = conditional_select()(oversold, "oversold", overbought, "overbought", "neutral")
