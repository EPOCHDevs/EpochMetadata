# Test time_of_day transform
# Detects if bars fall within specific UTC time ranges

src = market_data_source(timeframe="1H")

# Morning period: 09:00 - 12:00 UTC
morning_period = time_of_day(start="09:00", end="12:00")

# Afternoon period: 14:00 - 16:00 UTC
afternoon_period = time_of_day(start="14:00", end="16:00")

# Check if bars are in morning period - all outputs
is_morning = morning_period.active
morning_opened = morning_period.opened
morning_closed = morning_period.closed
morning_high = morning_period.high
morning_low = morning_period.low

# Check if bars are in afternoon period - all outputs
is_afternoon = afternoon_period.active
afternoon_opened = afternoon_period.opened
afternoon_closed = afternoon_period.closed
afternoon_high = afternoon_period.high
afternoon_low = afternoon_period.low
