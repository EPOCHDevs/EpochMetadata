# Test event_marker with EventMarkerSchema object construction
src = market_data_source(timeframe="1D")
signal = src.c > number(value=100).result
event_marker(
    event_marker_schema=EventMarkerSchema(
        title="Signal Alert",
        icon=Signal,
        select_key=SLOT0,
        schemas=[
            CardColumnSchema(column_id=SLOT0, slot=Hero, render_type=Boolean)
        ]
    )
)(signal)
