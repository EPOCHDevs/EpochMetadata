# Test intraday returns calculation
# intraday_returns uses open and close from the market data source directly

# Calculate simple intraday returns (close - open) / open
simple_returns = intraday_returns(return_type="simple")()

# Calculate log intraday returns log(close / open)
log_returns = intraday_returns(return_type="log")()
