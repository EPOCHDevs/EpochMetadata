# Test that data sources require explicit timeframe parameter
# This should fail compilation with an error

# This should fail: common_indices requires an explicit timeframe
spx = common_indices(index="SPX")()

# Use the data
result = spx.c
