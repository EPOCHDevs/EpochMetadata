period:
  from: "2024-01-01"
  to: "2024-01-05"

data:
  assets:
    - "AAPL-Stock"
  futures_continuation: null

# This test should fail during compilation because common_indices
# requires an explicit timeframe parameter
