# Source: strategies/ict-smart-money.md:70
# Section: ICT & Smart Money Concepts > Market Structure (BOS/CHoCH)
# Type: complete_strategy

src = market_data_source()

swings = swing_highs_lows(swing_length=5)(src.h, src.l)
structure = bos_choch()(swings.high_low, swings.level)

# BOS = trend continuation, CHoCH = potential reversal
bullish_bos = structure.bos == 1
bearish_choch = structure.choch == -1

# Combine with order blocks for high-probability setup
ob = order_blocks()(swings.high_low)
entry = bullish_bos and (ob.ob == 1) and (src.c >= ob.bottom)
exit = bearish_choch

trade_signal_executor()(
    enter_long=entry,
    exit_long=exit
)
