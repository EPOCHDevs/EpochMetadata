# Source: concepts/data-sources.mdx:205
# Section: Data Sources > Fundamental Data (Polygon)
# Type: complete_code

ratios = financial_ratios()
income = income_statement()

# Value screening
value = (ratios.pe < 15) & (ratios.pb < 2)

# Quality screening
quality = (ratios.roe > 0.15) & (ratios.debt_equity < 1)

# Yield screening
high_yield = ratios.div_yield > 0.03

# Combined filter
quality_value = value & quality & (income.net_income > 0)


trade_signal_executor()(enter_long=quality_value)
