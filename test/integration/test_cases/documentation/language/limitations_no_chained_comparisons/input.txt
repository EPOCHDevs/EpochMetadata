# Source: language/limitations.mdx:174
# Section: Limitations > No Chained Comparisons
# Type: complete_code

src = market_data_source(timeframe="1D")
rsi_val = rsi(period=14)(src.c)

# Use instead
above_30 = rsi_val > 30
below_70 = rsi_val < 70
valid = above_30 and below_70

# Or inline
valid_inline = (rsi_val > 30) and (rsi_val < 70)
