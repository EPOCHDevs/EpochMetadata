# Source: language/limitations.mdx:42
# Section: Limitations > No Control Flow
# Type: complete_code

# Boolean expression
signal = price > 100

# Ternary operator
signal = True if price > 100 else False

# conditional_select for multi-way branching
regime = conditional_select(
    rsi_val < 30, "oversold",
    rsi_val > 70, "overbought",
    "neutral"
)
