src = market_data_source(timeframe="1D")

# Create labels and values for bar chart
labels = text(value="EMA20")()
ema20 = ema(period=20)(src.c)

# Bar chart takes label and value inputs
bar_chart_report(title="Moving Averages", category="Technical", agg="last", x_axis_label="Indicator", y_axis_label="Value", vertical=False)(label=labels, value=ema20)
