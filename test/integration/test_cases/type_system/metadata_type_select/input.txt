# Test that Integer option parsing works correctly with ema
# ema period is an integer option
src = market_data_source(timeframe="1D")
result = ema(period=20)(src.c)
trade_signal_executor()(enter_long=result > 0)
