# Test that Decimal type parsing works correctly
# Using multiplication operator with number component
src = market_data_source(timeframe="1D")
result = src.c * number(value=2.5).result
trade_signal_executor()(enter_long=result > 0)
