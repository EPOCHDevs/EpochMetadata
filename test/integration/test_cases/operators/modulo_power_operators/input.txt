src = market_data_source(timeframe="1H")

# Modulo operator
mod_result = src.c % 10
mod_with_var = src.c % src.o
mod_negative = src.c % -3

# Power operator
pow_result = src.c ** 2
pow_with_var = src.c ** src.o
pow_fractional = src.c ** 0.5

# Combined with other operators
combined = (src.c % 2) == 0
signal = (src.c ** 2) > (src.o ** 2)

# Precedence test (** should have higher precedence than *)
precedence = 2 * src.c ** 2

# Signal output
trade_signal_executor()(enter_long=signal)
