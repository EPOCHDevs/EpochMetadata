# Test comparison operators in constant expressions
src = market_data_source()

# Comparison operators (note: these create lt/gt/etc transforms, not evaluated at compile-time when used as variables)
a = 15
b = 10

less_than = a < 20
greater_than = b > 5

# Use constants in subscripts
x = src.c[a]
y = src.v[b]
