# Test constant folding with mixed integer and float types
src = market_data_source(timeframe="1D")

# Mixed type arithmetic (evaluates to integers suitable for subscripts)
int_val = 10
result_int = 5 + int_val  # 15

# Use evaluated constants in subscripts
x = src.c[result_int]
y = src.v[int_val]
