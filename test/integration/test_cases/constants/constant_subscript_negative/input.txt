src = market_data_source(timeframe="1D")

# Negative periods for forward-looking lag
lookback = -5
future_close = src.c[lookback]

# Explicit unary minus operator
explicit_neg = -10
future_volume = src.v[explicit_neg]

# Use in calculation
result = future_close + future_volume
