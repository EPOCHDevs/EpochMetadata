src = market_data_source(timeframe="1D")

# Test constant folding in subscripts
lookback_period = 20
short_period = 10

# Using constant variables in subscripts
price_lag = src.c[lookback_period]
volume_lag = src.v[short_period]

# Constant arithmetic expressions
combined_period = 5 + 5
mixed_lag = src.h[combined_period]

# Test output
result = price_lag + volume_lag + mixed_lag
