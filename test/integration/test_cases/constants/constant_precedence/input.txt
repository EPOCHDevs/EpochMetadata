# Test operator precedence and parentheses in constant expressions
src = market_data_source(timeframe="1D")

# Test precedence: multiplication before addition
prec1 = 2 + 3 * 4      # Should be 14, not 20
prec2 = (2 + 3) * 4    # Should be 20

# Test with subtraction and multiplication
prec3 = 10 - 2 * 3     # Should be 4, not 24
prec4 = (10 - 2) * 3   # Should be 24

# Use in subscripts
result1 = src.c[prec1]
result2 = src.v[prec2]
result3 = src.h[prec3]
result4 = src.l[prec4]
