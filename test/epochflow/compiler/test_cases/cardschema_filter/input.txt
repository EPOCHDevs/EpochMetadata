this
# Test card_selector_filter with card_schema (CardSchemaFilter)
src = market_data_source()
signal = src.c > number(value=100).result
card_selector_filter(
    card_schema=CardSchemaFilter(
        title="Signal Alert",
        icon=Signal,
        select_key="signal#result",
        schemas=[
            CardColumnSchema(column_id="signal#result", slot=Hero, render_type=Boolean)
        ]
    )
)(signal)
