CompileFlags:
  # Remove problematic warnings if needed
  # Remove: -W*
  
  # Specify where to find compile_commands.json (list format for multiple directories)
  CompilationDatabase: cmake-build-debug 
  Add: [ -Wall, -Wextra, -Werror]

# Diagnostics:
  # Tweak clangd diagnostics if needed
  # ClangTidy:
  #   Add: [performance-*, modernize-*, readability-*]
  #   Remove: modernize-use-trailing-return-type
  
  # Uncomment if you want to disable clang-tidy completely